@page
@model ElasticSentinel.Pages.Queries.IndexModel
<div class="pagetitle">
    <h1>KQL Queries</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Home">Home</a></li>
            <li class="breadcrumb-item active">KQL Queries</li>
        </ol>
    </nav>
</div>
<hr />
<p>
    <a asp-page="Create" class="btn btn-primary" role="button">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ElasticQuery[0].QueryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ElasticQuery[0].QueryDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ElasticQuery[0].IsDynamic)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ElasticQuery)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.QueryName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QueryDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsDynamic)
                </td>
                <td>
                    <a class="btn btn-outline-primary btn-sm rounded-0" type="button" asp-page="./Edit" asp-route-id="@item.ElasticQueryId"><i class="bx bx-edit" title="Edit"></i></a> |
                    <a class="btn btn-outline-success btn-sm rounded-0" type="button" asp-page="./Details" asp-route-id="@item.ElasticQueryId"><i class="bx bx-detail" title="Details"></i></a>
                    @*<a asp-page="./Delete" asp-route-id="@item.ElasticQueryId">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>
