@page
@model ElasticSentinel.Pages.Queries.QueryResponse.IndexModel

<div class="pagetitle">
    <h1>Query Response</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Home">Home</a></li>
            <li class="breadcrumb-item">KQL Queries</li>
            <li class="breadcrumb-item active">Query Response</li>
        </ol>
    </nav>
</div>
<hr />
<p>
    <a asp-page="Create" class="btn btn-primary" role="button">Create New</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ElasticDynamicQueryResponseDetail[0].QueryResponseMapperName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ElasticDynamicQueryResponseDetail)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.QueryResponseMapperName)
                </td>
                <td>
                    <a class="btn btn-outline-primary btn-sm rounded-0" type="button" asp-page="./Edit" asp-route-id="@item.ElasticDynamicQueryResponseDetailId"><i class="bx bx-edit" title="Edit"></i></a> |
                    <a class="btn btn-outline-success btn-sm rounded-0" type="button" asp-page="./Details" asp-route-id="@item.ElasticDynamicQueryResponseDetailId"><i class="bx bx-detail" title="Details"></i></a>
                    @*<a asp-page="./Delete" asp-route-id="@item.ElasticDynamicQueryResponseDetailId">Delete</a>*@
                </td>
            </tr>
        }
    </tbody>
</table>
